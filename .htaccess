RewriteEngine On
RewriteBase /

# Se o arquivo ou diretório não existir, redireciona tudo para index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
